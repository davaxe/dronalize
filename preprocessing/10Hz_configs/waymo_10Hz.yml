# Description: Configuration file for the W(aymo) O(pen) M(otion) D(ataset)
description: Waymo Open Motion Dataset (WOMD)

dataset: waymo
seed: 42
sample_freq: 10
downsample: 1
skip_samples: 0
input_len: 1
output_len: 8
n_inputs: 7
n_outputs: 7

# Recordings
recordings: true